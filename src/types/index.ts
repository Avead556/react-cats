export interface Cat {
    image: string,
    title: string,
    description: string
}

export interface CourseItem {
    title: string;
    priceFormated: string;
    price: number;
    image: string;
    active: boolean;
}